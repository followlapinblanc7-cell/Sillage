# Optional cloud layer (private circle). Leave unset for local-only journal.
# Create a project at https://supabase.com — never commit real secrets.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key
